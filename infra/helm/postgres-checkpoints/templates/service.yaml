apiVersion: v1
kind: Service
metadata:
  name: postgres-checkpoints
spec:
  selector:
    app: postgres-checkpoints
  ports:
    - name: postgres
      port: {{ .Values.service.port }}
      targetPort: postgres
